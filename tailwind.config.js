export default {
   darkMode: "class",
   content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Adjust the paths as necessary
      "./public/index.html",
   ],
   plugins: [
      // other plugins
   ],
   // other configurations
};
